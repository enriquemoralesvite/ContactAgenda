---
---
<form id="formContact" class="max-w-sm mx-auto p-4 bg-white rounded-lg shadow">
  <title>Agenda de contactos</title>
  <h2 class="text-lg font-semibold mb-4">Agenda de Contacto</h2>

  <div class="mb-3">
    <label for="contactName" class="block mb-1 font-medium text-gray-700">Nombre:</label>
    <input
      type="text"
      id="contactName"
      name="contactName"
      placeholder="Nombre del Contacto"
      class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400"
      required
    />
  </div>

  <div class="mb-3">
    <label for="contactLastName" class="block mb-1 font-medium text-gray-700">Apellido:</label>
    <input
      type="text"
      id="contactLastName"
      name="contactLastName"
      placeholder="Apellido del Contacto"
      class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400"
      required
    />
  </div>

  <div class="mb-4">
    <label for="contactPhone" class="block mb-1 font-medium text-gray-700">Teléfono:</label>
    <input
      type="tel"
      id="contactPhone"
      name="contactPhone"
      placeholder="Número del contacto"
      class="w-full border border-gray-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400"
      required
    />
  </div>

  <div class="flex justify-between">
    <button
      type="button"
      id="btnAdd"
      class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600"
    >Agregar</button>
    <button
      type="button"
      id="btnEdit"
      class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600"
    >Editar</button>
    <button
      type="button"
      id="btnDelete"
      class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600"
    >Eliminar</button>
  </div>
</form>

<div id="toastContainer" class="fixed bottom-4 right-4 space-y-2"></div>